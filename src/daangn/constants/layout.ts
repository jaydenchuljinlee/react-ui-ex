export const FULL_WIDTH = 1400;
export const HEADER_HEIGHT = 64;

export const useResponsiveHeaderHeight = (): number => {
    return HEADER_HEIGHT;
};

export const useResponsiveFullWidth = (): number => {
    return FULL_WIDTH;
};